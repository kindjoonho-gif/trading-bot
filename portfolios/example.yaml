# Target weights — keys may be 6-digit code or Korean/English name.
# Sum must be <= 1.0. Residual = cash.
broker: KIS

holdings:
  "411060": 0.01     # ACE 금 ETF
  # residual 0.30 stays as cash

drift_tolerance: 0.01
